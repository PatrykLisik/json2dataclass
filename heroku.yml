build:
  docker:
    server: server/Dockerfile
    client: json2dataclass_client/Dockerfile
run:
  server:
    comand:
      - sanic server.sanic_app.app --host 0.0.0.0 --port $PORT
    image: server
  web:
    comand:
      - npm run serve
    image: web
